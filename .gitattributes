# 统一文本文件换行符为 LF，避免 core.autocrlf 在 Windows 上造成 CRLF/LF 混排与整文件 diff。
* text=auto eol=lf

# 明确二进制文件不做换行符转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.pdf binary
*.zip binary
*.gz binary
